<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>DELVIN X URL</title>
  <style>
    @import url('https://fonts.googleapis.com/css2?family=Montserrat:wght@600&display=swap');
    body {
      margin: 0;
      padding: 0;
      font-family: 'Montserrat', Arial, sans-serif;
      background: linear-gradient(135deg, #ff7272 0%, #c91818 100%);
      height: 100vh;
      display: flex;
      justify-content: center;
      align-items: center;
      overflow: hidden;
    }
    .watermark-box {
      background: rgba(255, 255, 255, 0.93);
      border: 8px solid #c91818;
      padding: 38px 32px 28px 32px;
      border-radius: 20px;
      text-align: center;
      color: #c91818;
      text-shadow: none;
      max-width: 480px;
      min-width: 340px;
      box-shadow: 0 0 30px #ffbaba85;
      position: relative;
      margin: 0 12px;
      font-size: 16px;
    }
    .title {
      font-size: 28px;
      color: #d90429;
      margin-bottom: 18px;
      font-weight: 700;
      letter-spacing: 1px;
      text-shadow: 0 2px 8px #ffbaba80;
    }
    .dl-icon {
      width: 48px;
      height: 48px;
      background: linear-gradient(135deg, #d90429 30%, #ff7272 100%);
      border-radius: 12px;
      box-shadow: 0 0 12px #ff7272c0, 0 0 4px #fff;
      position: absolute;
      top: -28px;
      left: -28px;
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 32px;
      color: #fff;
      animation: rotateIcon 5s linear infinite;
    }
    @keyframes rotateIcon {
      from { transform: rotate(0deg);}
      to { transform: rotate(360deg);}
    }
    .content {
      font-size: 15px;
      color: #a41313;
      line-height: 1.7;
      letter-spacing: 0.1px;
      margin-bottom: 0;
    }
    .highlight {
      color: #fff;
      background: #d90429;
      padding: 2px 8px;
      border-radius: 6px;
      font-weight: 600;
    }
    /* Responsive */
    @media (max-width: 500px) {
      .watermark-box { padding: 20px 8px 14px 8px; min-width: unset; }
      .title { font-size: 20px; }
      .dl-icon { width: 36px; height: 36px; font-size: 20px; top: -18px; left: -18px;}
    }
  </style>
</head>
<body>
  <div class="watermark-box">
    <div class="dl-icon">
      <i class="fas fa-exclamation-triangle"></i>
    </div>
    <div class="title">Unauthorized Usage Warning</div>
    <div class="content">
      This login UI was made by <span class="highlight">Delvine</span> and <span class="highlight">DelvineX</span>.<br>
      To use or replicate this page, you must obtain<br>
      explicit permission or purchase the <span class="highlight">Premium Version</span>.<br><br>
      &copy; 2025 DelvineX . All rights reserved.
    </div>
  </div>
  <!-- FontAwesome CDN for icon -->
  <script src="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/js/all.min.js"></script>
</body>
</html>
