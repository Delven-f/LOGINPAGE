<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>GTPS SERVER</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
  <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@600;700&display=swap" rel="stylesheet">
  <style>
    body {
      min-height: 100vh;
      background: transparent
      font-family: 'Montserrat', Arial, sans-serif;
      margin: 0;
    }
    .dashboard-led-wrap {
      position: relative;
      width: 28rem;
      max-width: 97vw;
      border-radius: 2rem;
      margin: 0 auto;
      box-shadow: 0 10px 32px 0 rgba(31,38,135,0.19);
      overflow: visible;
    }
    /* Blue-White LED border effect */
    .led-border {
      pointer-events: none;
      position: absolute;
      inset: -10px; /* Border thickness outward */
      z-index: 2;
      border-radius: 2.3rem;
      background: transparent;
    }
    .led-border::before {
      content: "";
      position: absolute;
      inset: 0;
      border-radius: inherit;
      z-index: 3;
      background: conic-gradient(
        from var(--angle, 0deg),
        #2196f3, #fff 15%, #2196f3 30%, #fff 45%, #2196f3 60%, #fff 75%, #2196f3 90%, #fff 100%
      );
      filter: blur(3.5px) brightness(1.18) drop-shadow(0 0 7px #fff5);
      animation: led-spin 2.7s linear infinite;
      -webkit-mask: 
        linear-gradient(#fff 0 0) content-box, 
        linear-gradient(#fff 0 0);
      -webkit-mask-composite: xor;
      mask-composite: exclude;
      pointer-events: none;
    }
    @keyframes led-spin {
      to { --angle: 360deg; }
    }
    /* Dashboard content panel */
    .glass-content {
      position: relative;
      z-index: 10;
      border-radius: 2rem;
      background: rgba(34,54,86,0.94);
      backdrop-filter: blur(8px);
      -webkit-backdrop-filter: blur(8px);
      padding: 2.7rem 2.2rem 1.2rem 2.2rem;
      box-shadow: 0 3px 28px 0 rgba(31, 38, 135, 0.13);
      display: flex;
      flex-direction: column;
      align-items: center;
    }
    /* Form and UI Styles */
    .header-title {
      font-size: 2.1rem;
      font-weight: 800;
      letter-spacing: 0.05em;
      color: #fff;
      text-align: center;
      margin-bottom: 2.2rem;
      filter: drop-shadow(0 4px 16px rgba(49,209,255,0.14));
    }
    .input-label {
      color: #e0e6ed;
      font-size: 1rem;
      font-weight: 600;
      margin-bottom: 0.2rem;
      letter-spacing: 0.01em;
      display: block;
    }
    .input-box {
      background: rgba(255,255,255,0.12) !important;
      border: 1.7px solid #2196f377 !important;
      border-radius: 0.9rem !important;
      color: #e0e6ed !important;
      font-size: 1.08rem;
      padding-left: 0.95rem;
      padding-right: 0.5rem;
      min-height: 2.6rem;
      transition: border-color 0.2s, box-shadow 0.2s;
      box-shadow: 0 1px 8px 0 rgba(49,209,255,0.05);
    }
    .input-box:focus-within { border-color: #fff; box-shadow: 0 0 0 2px #2196f366; }
    .input-box input:focus { outline: none; }
    .input-box::placeholder, .input-box input::placeholder { 
      color: #b3cce7; opacity: 1; font-size: 0.97rem; letter-spacing: .01em;
    }
    .btn-main {
      background: linear-gradient(90deg, #2196f3 0%, #fff 100%);
      color: #172043;
      font-weight: bold;
      border-radius: 1rem;
      box-shadow: 0 2px 16px #2196f355;
      transition: 0.2s;
      letter-spacing: 0.02em;
      border: none;
      font-size: 1.13rem;
    }
    .btn-main:hover, .btn-main:focus {
      background: linear-gradient(90deg, #fff 0%, #2196f3 100%);
      color: #172043;
      transform: translateY(-2px) scale(1.05);
      box-shadow: 0 4px 24px #2196f366;
      outline: none;
    }
    .btn-alt {
      background: linear-gradient(90deg, #fff 0%, #2196f3 100%);
      color: #172043;
      font-weight: bold;
      border-radius: 1rem;
      box-shadow: 0 2px 16px #2196f322;
      transition: 0.2s;
      letter-spacing: 0.02em;
      border: none;
      font-size: 1.13rem;
    }
    .btn-alt:hover, .btn-alt:focus {
      background: linear-gradient(90deg, #2196f3 0%, #fff 100%);
      color: #172043;
      transform: translateY(-2px) scale(1.05);
      outline: none;
    }
    .form-icon {
      color: #2196f3;
      margin-right: 0.85rem;
      font-size: 1.14rem;
      transition: color 0.2s;
    }
    .input-box:focus-within .form-icon { color: #fff; }
    .error-message {
      background: #fff2;
      color: #2196f3;
      border-radius: 0.6rem;
      padding: 0.5rem 1rem;
      font-size: 1rem;
      text-align: center;
      margin-bottom: 1.1rem;
      font-weight: bold;
      letter-spacing: 0.01em;
    }
    /* Credit bar */
    .credit-bar {
      display: flex;
      align-items: center;
      justify-content: center;
      gap: 0.4rem;
      margin-top: 2.1rem;
      margin-bottom: 0.8rem;
      color: #2196f3;
      font-size: 1.05rem;
      font-family: 'Montserrat', Arial, sans-serif;
      font-weight: 700;
      letter-spacing: 0.03em;
      opacity: 0.92;
      text-shadow: 0 2px 8px #2196f399, 0 0 2px #fff2;
      user-select: none;
    }
    .credit-bar .fa-crown {
      color: #fff;
      background: linear-gradient(135deg,#2196f3 50%, #fff 100%);
      border-radius: 50%;
      padding: 0.18em 0.23em;
      font-size: 1.09em;
      box-shadow: 0 0 5px #2196f399, 0 0 2px #fff;
      margin-right: 0.18em;
      animation: crown-flicker 1.25s infinite alternate;
    }
    @keyframes crown-flicker {
      0% { filter: brightness(1.08);}
      100% { filter: brightness(1.6);}
    }
    @media (max-width: 600px) {
      .dashboard-led-wrap,.glass-content { border-radius: 1.1rem !important; width: 99vw !important; }
      .glass-content { padding: 1.2rem !important; }
      .btn-main, .btn-alt { font-size: 1rem !important; padding: 0.7rem 1.1rem !important; }
      .input-box { font-size: 0.97rem; min-height: 2rem; }
      .header-title { font-size: 1.25rem;}
    }
  </style>
</head>
<body class="flex flex-col items-center justify-center min-h-screen">
  <div class="fixed inset-0 flex items-center justify-center">
    <div class="dashboard-led-wrap">
      <div class="led-border"></div>
      <div class="glass-content">
        <p class="header-title">GTPS SERVER</p>
        <div id="errorDiv" class="hidden error-message">
          <span id="errorMessage"></span>
        </div>
        <form 
          method="post"
          action="/player/growid/login/validate"
          accept-charset="UTF-8"
          id="loginForm" class="mt-3 w-full">
          <div class="mb-6">
            <label for="loginGrowId" class="input-label">GrowID</label>
            <div class="flex items-center input-box mt-1">
              <i class="fas fa-user form-icon"></i>
              <input type="text" name="growId" id="loginGrowId" 
                class="w-full bg-transparent border-none focus:outline-none placeholder-[#b3cce7] text-white"
                placeholder="Enter your growid" style="background-color: transparent;">
            </div>
          </div>
          <div class="mb-8">
            <label for="loginPassword" class="input-label">Password</label>
            <div class="flex items-center input-box mt-1">
              <i class="fas fa-lock form-icon"></i>
              <input type="password" name="password" id="loginPassword"
                class="w-full bg-transparent border-none focus:outline-none placeholder-[#b3cce7] text-white"
                placeholder="Enter your password" style="background-color: transparent;">
              <button type="button" id="toggleLogPassword" class="ml-3 bg-transparent border-none focus:outline-none">
                <i class="fas fa-eye text-[#2196f3]"></i>
              </button>
            </div>
          </div>
          <div class="flex justify-between items-center mt-7">
            <button id="loginButton" type="submit" class="btn-main text-lg px-7 py-2.5">
              Login
            </button>
            <a href="/register">
              <button type="button" class="btn-alt text-lg px-7 py-2.5">
                Register
              </button>
            </a>
          </div>
        </form>
        <!-- Credit Logo/Bar -->
        <div class="credit-bar mt-7">
          <i class="fas fa-crown"></i> 
          <span>Growtopia Private Server</span>
        </div>
      </div>
    </div>
  </div>
  <script>
    document.addEventListener('DOMContentLoaded', function () {
      if (localStorage.getItem('growId')) {
        document.getElementById('loginGrowId').value = localStorage.getItem('growId');
      }
    });

    document.getElementById('loginButton').addEventListener('click', function (event) {
      event.preventDefault();
      const loginForm = document.getElementById('loginForm');
      const uName = document.getElementById('loginGrowId').value;
      const uPass = document.getElementById('loginPassword').value;
      if (!uName || !uPass) {
        document.getElementById('errorDiv').classList.remove('hidden');
        document.getElementById('errorMessage').innerText = 'Username or Password is empty';
        return;
      } else if (uName.length < 4 || uPass.length < 4) {
        document.getElementById('errorDiv').classList.remove('hidden');
        document.getElementById('errorMessage').innerText = 'Username or Password must be at least 4 characters long';
        return;
      } else {
        loginForm.submit();
        localStorage.setItem('growId', uName);
      }
    });

    document.getElementById('toggleLogPassword').addEventListener('click', function () {
      const LpasswordField = document.getElementById('loginPassword');
      const LpasswordFieldType = LpasswordField.getAttribute('type');
      if (LpasswordFieldType === 'password') {
        LpasswordField.setAttribute('type', 'text');
        this.innerHTML = '<i class="fas fa-eye-slash text-[#2196f3]"></i>';
      } else {
        LpasswordField.setAttribute('type', 'password');
        this.innerHTML = '<i class="fas fa-eye text-[#2196f3]"></i>';
      }
    });
  </script>
</body>
</html>
