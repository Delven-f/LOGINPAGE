<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>DelvineX - Dashboard</title>
    <!-- Font Pixel -->
    <link href="https://fonts.googleapis.com/css2?family=Press+Start+2P&display=swap" rel="stylesheet">
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
        .pixel-font {
            font-family: 'Press Start 2P', cursive !important;
            image-rendering: pixelated;
            letter-spacing: 1px;
        }
        .pixel-card {
            border: 4px solid #222;
            box-shadow: 0 0 0 4px #fff, 0 8px 32px 0 rgba(31,38,135,0.10);
            border-radius: 0;
            background: #e0e0e0;
        }
        .pixel-btn {
            font-family: 'Press Start 2P', cursive !important;
            border: 2px solid #222;
            border-radius: 0;
            background: #00b8ff;
            color: #fff;
            padding: 10px 32px;
            cursor: pointer;
            text-transform: uppercase;
            font-size: 12px;
            margin: 4px;
            transition: background 0.2s, transform 0.2s;
        }
        .pixel-btn:hover {
            background: #06a4dd;
            transform: scale(1.1);
        }
        body {
            background: #b8eaff;
            min-height: 100vh;
        }
        /* Tambahan gaya untuk tabel pixel */
        .pixel-table {
            font-family: 'Press Start 2P', cursive !important;
            border-collapse: collapse;
            width: 100%;
        }
        .pixel-table th, .pixel-table td {
            border: 2px solid #222;
            padding: 8px;
            background: #fff;
            font-size: 10px;
        }
        .pixel-table th {
            background: #ffcc00;
        }
    </style>
</head>
<body>
    <div class="flex items-center justify-center min-h-screen w-full px-2">
        <div class="pixel-card max-w-2xl w-full p-8 flex flex-col items-center">
            <!-- Logo Pixel (opsional) -->
            <div class="mb-4">
                <img src="/path/to/your/pixel-logo.png" alt="Logo" style="width: 80px; image-rendering: pixelated;">
            </div>
            <!-- Judul Dashboard -->
            <h1 class="pixel-font text-2xl mb-2 text-[#222]">DASHBOARD GTPS</h1>
            <!-- Deskripsi Dashboard -->
            <p class="pixel-font text-xs mb-6 text-[#444] text-center">
                Selamat datang di server GTPS kamu!<br>
                Semua aktivitas dan informasi akunmu tersedia di bawah ini.
            </p>
            <!-- Contoh Tabel User Info -->
            <table class="pixel-table mb-6">
                <tr>
                    <th>GrowID</th>
                    <td><%= user.growId %></td>
                </tr>
                <tr>
                    <th>Email</th>
                    <td><%= user.email %></td>
                </tr>
                <tr>
                    <th>Level</th>
                    <td><%= user.level %></td>
                </tr>
                <!-- Tambahkan baris lain sesuai kebutuhan -->
            </table>
            <!-- Contoh tombol pixel -->
            <div class="flex flex-wrap justify-center">
                <a href="/logout"><button class="pixel-btn">LOGOUT</button></a>
                <a href="/profile"><button class="pixel-btn">PROFILE</button></a>
                <!-- Tambahkan tombol lain sesuai kebutuhan -->
            </div>
        </div>
    </div>
</body>
</html>
